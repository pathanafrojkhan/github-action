name: Caching with npm

on: push

jobs:
  first:
    runs-on: ubuntu-latest
    steps:
      - run: echo "first"
  second:
    needs: first
    runs-on: ubuntu-latest
    steps:
      - run: echo "second"
  third:
    needs: second
    runs-on: ubuntu-latest
    steps:
      - run: echo "third"
