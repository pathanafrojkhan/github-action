name: Caching with npm

on: push

jobs:
  first:
    runs-on: ubuntu-latest
    env:
      test: Hello
    steps:
      - id: json_data_first
      - run: echo "$test is $test1"
        env:
          test1: Mona
  second:
    needs: first
    runs-on: ubuntu-latest
    steps:
      - id: second
      - run: echo "$test"
  third:
    needs: second
    runs-on: ubuntu-latest
    steps:
      - id: third
      - run: echo "third"
